
/*

Core.js

This file contains functions that will be called by host.js when the page loads.

*/

var Core = {
  registry: {
    get: function () {
      // fetch the address and uuid from the DOM
      return _.all();
    },
    contact: function () {
      return this.get().then(function () {
        
      });
    },
  }
}
